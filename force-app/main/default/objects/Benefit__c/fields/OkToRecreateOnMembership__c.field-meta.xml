<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>OkToRecreateOnMembership__c</fullName>
    <defaultValue>false</defaultValue>
    <description>Used by automations to decide if the benefit can be recreated on a membership if there already is one similar benefit that is already closed.</description>
    <inlineHelpText>Used by automations to decide if the benefit can be recreated on a membership if there already is one similar benefit that is already closed.</inlineHelpText>
    <label>Får återskapas på samma medlemskap</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
